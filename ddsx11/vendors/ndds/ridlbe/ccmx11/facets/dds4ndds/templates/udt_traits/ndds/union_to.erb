switch (from._d ())
{
% members.each do |_m|
%   if _m.is_default?
  default:
%   else
%     _m.nondefault_labels.each do |_l|
  case <%= _l %>:
%     end
%   end
  {
    ::DDSX11::to_dds (to._u.<%= _m.cxxname %>, from.<%= _m.cxxname %> ());
    break;
  }
% end
}

::DDSX11::to_dds (to._d, from._d ());

return to;
