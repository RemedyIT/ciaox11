  // generated from <%= ridl_template_path %>
% if all_incl_receptacle_port?

  // Always require remote facet reference, replacing local executor
  connection = CIAOX11::Corba::Utility::get_provider_reference (config);
  if (!connection)
  {
    // return here to prevent an inadvertent disconnect
    return true;
  }

% else
  X11_UNUSED_ARG(config);
% end
