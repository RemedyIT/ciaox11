/**
* @file    <%= idl_name %>.idl
* @author  Unknown
*
* Generated by 'brix11 axp prepare' @ <%= Time.now %>
*/
#ifndef <%= def_name %>
#define <%= def_name %>

#include <Components.idl>
<%= ttc_idl %>

% included_idl.each do |i|
#include "<%= i %>"
% end
% unless pragmas.empty?

%   pragmas.each do |p|
#pragma <%= p %>
%   end
% end

% module_names.each do |u|
module <%= u %>
{
%   inc_nest
% end
component <%= component_name %>
{
% ports.each do |p|
  <%= p.type %> <%= p.intf_name %> <%= p.name %>;
% end
% attributes.each do |a|
  <%= a.readonly ? 'readonly ' : '' %>attribute <%= a.type %> <%= a.name %>;
% end
};
% module_names.each do |_|
%   dec_nest
};
% end
#endif /*  <%= def_name %>  */
