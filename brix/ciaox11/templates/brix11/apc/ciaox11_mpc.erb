// Generated by 'brix11 axp prepare' @ <%= Time.now %>

% projects.each do |p|
project(<%= p.project_name %>) : <%= p.base_projects.join(', ') %> {
%   if !p.project_dependencies.empty?
  after += <%= p.project_dependencies.join(' ') %>
%   end
%   nest { p.visit_specific_section }
}

% end
